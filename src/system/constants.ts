export const DELIVERY_EXPIRY_MINUTES = 60; // Delivery expires if not accepted within 60 mins
export const PICKUP_TIMEOUT_MINUTES = 45;    // Delivery abandoned if not picked up 45 mins after acceptance
export const STALE_DELIVERY_HOURS = 4;       // Delivery flagged if not completed 4 hours after pickup
